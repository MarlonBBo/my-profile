import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};

module.exports = {
  images: {
    domains: ['avatars.githubusercontent.com', 'camo.githubusercontent.com', 'i.imgur.com'],
  },
};

export default nextConfig;
